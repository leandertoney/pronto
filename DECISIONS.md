# DECISIONS

Judgment calls made while building the MVP in one shot, per the brief's "ask zero questions" instruction.

## Stack & libraries

- **Expo SDK 57** (latest stable at build time) with the blank TypeScript template plus expo-router added; `main` is `expo-router/entry`. **Later downgraded to SDK 54** (see the "Downgrade to Expo SDK 54" commit) to match the installed Expo Go app version — SDK 54 is what's actually running; this entry is kept for history.
- **`expo-audio` over `expo-av`** for recording — it's the current, supported audio API on modern Expo SDKs (expo-av is deprecated) and works in Expo Go.
- **Raw `fetch` for both OpenAI and Anthropic** instead of their SDKs. Keeps the bundle small, avoids any Node-shim risk inside Expo Go, and the brief's contract (send full history, strip fences, defensive parse) is inherently manual anyway. The Anthropic call includes the `anthropic-dangerous-direct-browser-access` header, which is accurate: this is a client-side key for personal testing (see README warning).
- **Model `claude-sonnet-4-6`** exactly as specified in the brief (verified as a current, active Anthropic model ID).
- **Zustand** holds the whole conversation state machine (`src/store/useConversation.ts`); the screens are thin views over it.

## Design

- **Guatapé palette (revised 2026-07-17, by request).** The original build followed the brief's "dark, cinematic" direction; Leander asked for something that feels fun and colorful like Hispanic culture — he's headed to Medellín, with Guatapé nearby. The redesign takes the zócalo houses of Guatapé as the reference: warm whitewashed-cream background (`#FFF7EC`) with three saturated trim colors — zócalo coral (`#F25C3A`, CTA + mic), sunshine yellow (`#FFB627`), and turquoise (`#12A5A0`). Tasteful means the cream canvas does most of the work and the color stays in the trim: bubbles are soft tints (turquoise for Spanish, sunshine for the user), text is deep coffee brown, and only the mic/CTA is fully saturated.
- **Wave motif** is now a three-color zócalo stripe (coral, sunshine, turquoise bars) on the Home screen, with "onda" picked out in coral in the wordmark.
- **Score ring** is a color-coded circular badge with the numeric score (green ≥80, amber 50–79, red <50). A true animated progress ring would need `react-native-svg`; the simple ring satisfies "simple ring/percentage" without another dependency.

## Loop mechanics

- **Hands-free conversation (revised 2026-07-17, by request).** The original tap-to-start/tap-to-stop mic felt like a walkie-talkie, not a conversation. Now the app auto-starts listening whenever it's the user's turn and uses metering-based voice activity detection (expo-audio `isMeteringEnabled`): speech above −35 dB marks the utterance started; 1.3 s below −40 dB after speech ends it and sends to Whisper. Guards: 8 s of pure silence recycles the listener, 25 s hard cap per utterance, sub-0.5 s recordings are discarded. The mic button remains as "send now"; a header toggle switches back to tap-to-talk (`auto 🎙️` / `tap 🎙️`). Same recording routes to English-input or Spanish-repeat depending on whether a target phrase is active.
- **Scoring**: Whisper transcribes the attempt locked to `language: "es"`, then token-level Levenshtein over normalized text (lowercase, accents/punctuation stripped) mapped to 0–100. Tiers: ≥80 "¡Perfecto!", 50–79 replay + retry, <50 slow replay + retry. **Max 2 retries**, then the app moves on positively (phrase is still saved with its best score) and extends anyway — no failure dead-end, matching the "no red pen" vibe.
- **Extensions** are requested from Claude with an explicit instruction to repeat the core phrase and add exactly one element; the extension becomes the new target and the loop continues indefinitely.
- **Phrase persistence**: a phrase is saved to AsyncStorage when the user completes it (perfect score or moved-on after retries), deduplicated by Spanish text keeping the best score. This is the spaced-repetition seed; no review UI in MVP (out of scope).
- **Claude history**: assistant turns are stored as the parsed JSON (re-serialized) rather than raw model output, so a malformed-then-retried reply never pollutes the history.

## Voice

- **Natural Spanish voice via OpenAI TTS (revised 2026-07-17, by request).** The original build used `expo-speech` for everything, but the device's default Spanish voice sounds robotic — not the native Latina voice Leander wanted. Spanish phrases now go through OpenAI's `gpt-4o-mini-tts` with the `nova` voice (warm younger woman), steered toward Latin American / Colombian Spanish via the `instructions` field. ElevenLabs was considered and rejected — its free tier is a tiny one-time trial and bans commercial use, so it's not viable without paying.
- **On-device audio cache** (`src/services/openaiTts.ts`): TTS is deterministic, so each phrase's MP3 is saved to `Paths.cache/tts-cache/` under a stable DJB2-hash filename keyed by normalized text + voice. Replays hit the cached file for free; only brand-new phrases call the API. At ~$0.0006/phrase this keeps a whole trip's practice to a few cents. The 0.6× **slow replay reuses the same cached file** via `setPlaybackRate` — no second API call.
- **Graceful fallback**: if the TTS API or playback fails, `speakSpanish` falls back to the free device Spanish voice (still resolved es-MX → es-ES → es-*), so a phrase is always spoken even offline.
- **One voice everywhere (revised 2026-07-17, by request).** Leander wanted a single consistent voice; the mismatch (natural Spanish + robotic device English) sounded "creepy." English coaching lines now also go through OpenAI `nova`, steered to warm American English via a per-language `instructions` map. Same cache mechanism, keyed by text + language + voice (so the same word spoken in es vs en caches separately). Device voice remains only as the offline fallback (Spanish rate 0.85, slow 0.6).

## Whisper hallucination handling (2026-07-17)

Whisper hallucinates YouTube-caption boilerplate ("thanks for watching", "gracias por ver", "please subscribe", "you") when fed silence or near-silence. In hands-free mode this created a broken loop: a near-empty clip → Whisper invents "thanks for watching" → Claude translates to "gracias por ver" → the app coaches a phrase the user never said (with its Replay/Slow buttons, which the user heard as the app "saying Replay or slow"). Fixes:
- **`src/lib/whisperHallucination.ts`**: normalizes the whole transcript (lowercase, strip accents/punctuation) and, if it exactly matches a known hallucination phrase (or is empty), treats it as "nothing said" (returns ''). Matches the full transcript, never substrings, so a real sentence containing "gracias" survives.
- **Whisper call** now sends `temperature: 0` for deterministic decoding (less prone to hallucination) and passes the transcript through the filter.
- **VAD hardening** in the conversation screen: only transcribe when sustained speech was actually detected (`heardSpeech`) AND the clip is ≥700ms; the auto-listen start is delayed 550ms so the mic doesn't catch the tail of the app's own TTS (which would feed Whisper garbage). Together these keep silent clips out of Whisper entirely, with the phrase filter as the backstop.
- **Audio session juggling**: `allowsRecording` is enabled only while actually recording and disabled right after stopping, so TTS playback comes out of the main speaker at full volume (iOS routes audio to the quiet earpiece when a recording session is active).

## UI overhaul v2, "Pronto" refresh (2026-07-20)

Full presentation-layer renovation per `ui-redesign-prompt-v2.md`, built against `mockups-v2.html` and grounded in `CURRENT_STATE.md`. Functionality was explicitly frozen for this round: `useConversation.ts` (the whole state machine, VAD/recording logic, retry/scoring/phase mechanics), `audioSession.ts`, `whisper.ts`, `claude.ts`'s network logic, `tts.ts`/`openaiTts.ts`, `similarity.ts`, `phraseStore.ts`, and `wordStore.ts` were not touched except for the two narrowly-scoped copy changes the spec explicitly allowed. Verified via `git diff` on `useConversation.ts`: zero changes.

**Rename.** New `src/constants/brand.ts` holds the app name (Pronto), subtitle (AI Spanish Tutor), and tagline (Spanish for right now.) as the single source of truth for a future rename. `app.json`'s `name` field updated to "Pronto"; `slug` and `scheme` deliberately left as `que-onda`/`queonda` per the spec, so the installed dev client setup (Expo Go pairing, any cached deep links) doesn't break. **Left alone, not asked about**: the iOS/Android mic-permission strings in `app.json` and their `NSMicrophoneUsageDescription` still say "Qué Onda listens to you speak..." — the spec's rename scope was specific (screen titles, nav copy, the brand.ts constants) and didn't call these out, so they were left as a minor inconsistency rather than assumed in scope. **Deviation from `mockups-v2.html`**: the mockup renders the Home wordmark as "pron" + bold/accent-colored "to" (`pron<b>to</b>`). Leander flagged that splitting the word visually (color change mid-word) reads as if "pron" and "to" are two separate syllables meant to be read apart, which is confusing. Reverted to a single-color, single-weight wordmark; `APP_NAME_SPLIT` removed from `brand.ts`, `APP_NAME` rendered as one string in `index.tsx`.

**Component extraction (`src/components/`).** Eight shared primitives: `AppText` (variant-based theme font application), `Card` (the white bordered/optionally-shadowed/optionally-colored-border container used by score cards, the next-step card, and stat tiles), `PrimaryButton`/`OutlineButton`, `ScoreRing` (centralizes score-band coloring via an exported `scoreColorFor`, replacing three separately-duplicated copies of the same `>= 80 / >= 50` logic across conversation.tsx and profile.tsx), `ScreenHeader` (back chevron in a card square + centered title + right slot), `ZocaloMark` (static, size-prop-driven), and `ListenBars` (the new living equalizer, detailed below). All four screens now import from this directory; nothing screen-local reimplements a full-size button, card, or score ring. Small one-off shapes that are genuinely not one of those primitives (list-row containers, chip/pill/badge shapes, the mode-pill toggle, icon squares) were deliberately kept as local screen styles rather than force-fit into a shared component, matching how the mockup itself treats them as distinct, screen-specific CSS classes rather than one shared "Card" class.

**Typography.** `expo-font` plus `@expo-google-fonts/bricolage-grotesque` and `@expo-google-fonts/instrument-sans` installed via `npx expo install` (both load fine in Expo Go, no dev client needed). Fonts load in `_layout.tsx` with `expo-splash-screen`'s `preventAutoHideAsync`/`hideAsync` pattern, returning `null` until `useFonts` resolves, so there is no flash of an unstyled system font. `theme.ts`'s `fonts` object updated to the spec's 5 variants (display/title/body/caption/button) plus **two judgment-call additions not in the original 5-variant list**: `captionItalic` (a real italic font file, `InstrumentSans_500Medium_Italic`) and `bodyBold` (`InstrumentSans_700Bold`). Both were necessary because custom `fontFamily` values don't reliably synthesize `fontStyle: 'italic'` or a bolder `fontWeight` the way the system font does on native, and the app has extensive italic bilingual-subtitle text plus one bolded tagline phrase on Home that the original 5 variants had no way to render correctly. `expo-splash-screen` was also newly installed (not previously a dependency) since the spec calls for a splash hold and nothing in the project provided one yet.

**ListenBars, the signature element.** Built entirely on React Native's built-in `Animated` API (no Reanimated, no new dependency), per the spec's explicit instruction. Four states (`rippling`/`breathing`/`frozen`/`hidden`) map from the conversation screen's `phase` plus `autoListen`: rippling while actually recording, a slow synchronized breathe while auto-listen is armed and waiting for the user's turn (awaiting-english/awaiting-repeat/choosing), frozen at a low fixed height while busy (transcribing/thinking/scoring), hidden otherwise (tap mode off and idle, matching the spec's explicit hidden condition). Reduced-motion is read once via `AccessibilityInfo.isReduceMotionEnabled()` and re-subscribed via the `reduceMotionChanged` event; when active, bars freeze at a mid-height resting scale regardless of state (except `hidden`, which still hides). **One implementation change from the first draft**: the breathing state originally tried to make 4 follower bars track a single leading bar's animated value via `addListener`, but `useNativeDriver: true` animated values update on the native thread, so a JS-side listener reading them back can visibly lag or desync; simplified to 5 independent but identically-timed loops instead, which is simpler and has no cross-thread synchronization risk.

**Icons.** `@expo/vector-icons` (bundled with Expo, confirmed zero extra native setup or config plugin needed) supplies structural chrome: the back chevron (`ScreenHeader`), the mic icon, the play icon on Replay buttons, and the close icon on remove buttons. Emoji was kept everywhere the spec called it expressive content: celebration lines, the five extend/new-topic/progress chip emoji, and the turtle on every Slow/slow-replay control (explicitly named in the spec as staying). The mode-pill auto/tap toggle previously used a 🎙️ emoji next to each label; since the mockup's version of this toggle is plain text with no emoji, and it reads as structural chrome (a settings toggle) rather than expressive content, the emoji was dropped there.

**Conversation screen's one mapping change.** Per spec: `user-english` bubbles now show only the English text (their Spanish translation is dropped, since the `spanish` card that follows the same turn already delivers that translation and showing it twice would kill the reveal); `user-attempt` bubbles now show only the transcribed Spanish (their English gloss is dropped, since the score card's "why" box that follows already covers what was heard). This is a reversal of the previous session's "show a bilingual subtitle on every kind of line" feature for these two specific kinds only. `coach` and `spanish` kinds keep their translations exactly as before. The "why" box was promoted from fine print to a featured element per spec: a labeled section ("WHAT I HEARD, WORD BY WORD") with per-word hit/miss chips (turquoise-tinted for hits, soft coral with an underline for misses) followed by the italic raw-transcript line, inside the same `Card` as the score ring and verdict rather than a plain nested box.

**Storage keys and scheme/slug.** Both AsyncStorage keys (`@queonda/learned-phrases`, `@queonda/word-dictionary`) were left completely unchanged, as the spec required, so no existing user data is lost by the rename. `app.json`'s `slug`/`scheme` were likewise left as `que-onda`/`queonda` for the same continuity reason, noted above.

**Copy sweep (em/en dashes).** Re-verified after this round's changes: zero em or en dashes remain in any rendered or spoken user-facing string across the touched files. The Claude system prompt's existing "never use em dashes or en dashes" rule (added last session) was left as-is rather than reworded to the spec's more literal "in any field of the JSON reply" phrasing, since the entire model reply *is* the JSON object under this contract, so the two phrasings enforce the identical behavior, and the spec explicitly said to change nothing else in that prompt. **One deliberately unresolved item, consistent with last session's precedent**: `openaiTts.ts`'s `VOICE_ANCHOR` string (sent to OpenAI's TTS API as voice-steering instructions, not the Claude prompt) still contains one em dash. The spec named the Claude system prompt specifically, not the TTS instructions, so this was left untouched pending an explicit ask, exactly as it was flagged and left in the previous session.

**Verification.** `npx tsc --noEmit` clean, `npx jest` 70/70 tests passing across all 7 suites (none needed changes, since no pure-logic file was touched), `npx expo-doctor` 18/18 after the four new dependency installs (`expo-font`, `@expo-google-fonts/bricolage-grotesque`, `@expo-google-fonts/instrument-sans`, `expo-splash-screen`; `@expo/vector-icons` was already a transitive Expo dependency and is now pinned explicitly).

## Em dash / en dash sweep of user-facing text (2026-07-19)

Leander asked for every em dash (—) and en dash (–) in user-facing strings, UI copy, and the Claude system prompt to be replaced with a comma, period, or natural rewrite, plus an explicit rule added to the system prompt banning them going forward.

**Scope discipline**: dashes inside code *comments* (JSDoc blocks, inline `//` explanations) were deliberately left untouched — those are for developers reading the source, not user-facing text, and the request was specifically about strings the user sees or hears, plus the system prompt. Rewriting every comment in the codebase would have been a much larger, out-of-scope change.

**Fixed:**
- **Claude system prompt** (`src/services/claude.ts`): every em dash in the prompt text itself removed (rewritten with commas/periods, e.g. "Output contract — CRITICAL" → "Output contract, CRITICAL"). Added an explicit new style rule: "Never use em dashes or en dashes in any response. Use a comma, period, or parentheses instead."
- **Rendered/spoken UI strings**: the mic-permission explanation, all `MIC_LABELS` phase labels, the "Add where" chip's element text, every bilingual celebration/feedback line pair (`PERFECT_LINES`, `MOVING_ON_LINE`, `feedbackFor`), every store-level error message (`"I couldn't hear that..."`, `"Something went wrong..."`), the `env.ts` missing-API-key error messages, and the `elementInstruction` text sent to Claude when a chip specifies an extend element (`useConversation.ts` and its `nextCommand.ts` counterpart, kept consistent with each other).
- **Numeric placeholder**: `profile.tsx`'s average-score tile showed an em dash as a "no score yet" placeholder; changed to `'--'` (a plain double hyphen), since that's a value display rather than prose and doesn't want punctuation-style rewriting.
- **Test fixture** (`__tests__/nextCommand.test.ts`) updated to match the corrected chip-element string, since it had asserted the old em-dash text verbatim.

**Explicitly left alone** (flagged for Leander's awareness, not fixed since it's outside the stated scope): `src/services/openaiTts.ts`'s `VOICE_ANCHOR` string, sent as the `instructions` parameter to OpenAI's TTS API, still contains an em dash. It's a prompt sent to an AI model much like the Claude system prompt is, but the request named the Claude system prompt specifically, not the TTS instructions, so it was left as-is pending an explicit ask.

`npx tsc --noEmit` and `npx jest` (70 tests, 7 suites) both pass clean after the sweep.

## Pre-overhaul discrepancy cleanup (2026-07-19)

`CURRENT_STATE.md` (the UI-overhaul baseline doc) flagged four small issues to resolve before starting the redesign. All four fixed:

- **SDK version discrepancy**: ran `npx expo-doctor` to find the *actual* failing check, which turned out to be a missing peer dependency (`expo-audio` requires `expo-asset`, which wasn't installed) rather than the README/DECISIONS.md "SDK 57" text itself, since `expo-doctor` doesn't check markdown prose. Installed `expo-asset` via `npx expo install expo-asset` (correctly added as both a dependency and an `app.json` config plugin, since `expo-asset` ships a real plugin). `expo-doctor` now reports 18/18 checks passing. Separately corrected README.md's stale "SDK 57" to "SDK 54" (matching the actually-installed `expo: ^54.0.36`), and annotated the SDK 57 references earlier in this file as historical rather than deleting them, since a prior commit explicitly downgraded 57→54 to match the installed Expo Go app version.
- **Unused `@/*` path alias**: removed from `tsconfig.json` entirely, no file in the project used it.
- **Unused `stopSpeaking()` export**: removed from `src/services/tts.ts`. Its `expo-speech` import (`Speech`) is still used elsewhere in the same file, so no dangling import resulted.
- **`expo-constants`/`expo-linking` question**: confirmed these are legitimate transitive/config-only dependencies (not dead weight), left as-is.

`npx tsc --noEmit`, `npx jest` (70 tests), and `npx expo-doctor` (18/18) all pass clean after these fixes.

## Score-row subtitle was missing entirely, and the first fix mistranslated (2026-07-19)

Leander's screenshot showed the bilingual-subtitles feature working correctly for coach lines (confirming Claude's `coach_line_spanish` field genuinely works on live traffic — a real win, since that was unverified going in) — but the score row itself ("¡Eso es! That was spot on.") had no separated translation at all, because its text came from `PERFECT_LINES`/`feedbackFor`, hardcoded strings I never touched when adding the subtitle feature. Those strings blended Spanish and English into one line (a Spanish exclamation + a distinct English sentence) rather than being two versions of the same content.

First pass at fixing this mechanically split each blended string into `{en, es}` at the punctuation boundary — which produced five pairs that are NOT translations of each other (e.g. `{es: "¡Eso es!", en: "That was spot on."}` — "¡Eso es!" actually means "That's it!", not "that was spot on"). In an app whose stated purpose is correlating the two languages, that would have silently taught a false correspondence on the exact line Leander flagged. Caught before committing and corrected: `PERFECT_LINES` now holds five faithful Spanish/English pairs (¡Perfecto!/Perfect!, ¡Eso es!/That's it!, ¡Qué bien!/Nicely done!, ¡Increíble!/Incredible!, ¡Así se hace!/That's how it's done!). `feedbackFor`'s close/retry lines and `MOVING_ON_LINE` were already faithful translations from the start — no change needed there.

The `score`-kind transcript entry now carries `spanishTranslation` like every other entry, rendered as a second line under the score text (`app/conversation.tsx`'s `ScoreRow`). Perfect-score celebrations are now spoken in both languages (English then Spanish) — confirmed with Leander as an intentional change, not a side effect: more Spanish exposure at the exact moment a phrase was nailed, consistent with how the greeting/new-topic/next-commands-tip are already spoken bilingually elsewhere in the app.

## Bilingual subtitles — reverse translation under every line (2026-07-19)

Leander wanted every conversational line to show its reverse translation underneath — if she says Spanish, show English under it; if she (or the coach text) says English, show Spanish under it — since some of the learning happens by reading along, not just listening.

- **The `spanish`-kind bubble already had this** (Spanish phrase + `englishMeaning` underneath) — no change needed there.
- **Claude's JSON contract gained two new fields**: `coach_line_spanish` (Spanish translation of the English coach line) and `user_input_spanish` (Spanish translation of the user's own English utterance that turn; empty string on an EXTEND call, since there's no fresh user utterance then). Both parsed defensively like `words` — default to `''` if missing/non-string, never fail the whole reply.
- **`coach` entries** now render the English coach line with the Spanish translation in italic turquoise underneath.
- **`user-english` entries** (what the user said) get their Spanish translation from `user_input_spanish`, patched onto the transcript entry by id once Claude's reply arrives (the entry is created before the reply exists, so it starts without a translation and gets updated in the same `set()` that adds the reply's own entries).
- **`user-attempt` entries** (the user's Spanish repeat) show `target.english` underneath — the meaning they were *aiming for*, not a literal re-translation of whatever Whisper heard, so it stays useful even when the attempt wasn't perfect.
- **The greeting and "¿Qué más?" new-topic lines** are hardcoded bilingual strings (not from Claude) — split into a proper `text`/`spanishTranslation` pair instead of one concatenated blob string, so they render the same way as everything else.
- **`max_tokens` bumped 1024→2048** — the JSON reply payload has grown across this session's features (`words[]`, now two more sentence fields); for a longer extended phrase this was getting close to truncation risk, which would throw a parse error and burn a retry.
- **Unverified from here**: whether the live Claude model reliably fills `coach_line_spanish`/`user_input_spanish` on every real reply (the parser only tolerates their absence, tests only exercise hand-authored JSON). The greeting/new-topic subtitles are hardcoded and will always show regardless — so on device, check a *coach line* and *the user's own English line* specifically, not just the greeting, to know if this is actually working end-to-end.
- **Known, deliberately unfixed cosmetic quirk**: the greeting speaks Spanish first but now displays English-primary/Spanish-subtitle (matching every other `coach` entry's field meaning) — a minor inversion of speech order vs. display order. Left as-is rather than special-casing the greeting, since it's not clear it's actually a problem worth the inconsistency; happy to swap it if it reads oddly in practice.

## Quiet/inconsistent greeting volume — first-line-only fix, not the whole story (2026-07-19)

Leander reported the opening greeting ("¿Qué onda? What are you doing right now?") played back quiet and inconsistent in volume. Root cause for that specific line: `exitRecordingMode()` (which sets `allowsRecording: false` so iOS routes audio to the main speaker instead of the quiet earpiece) was only ever called inside `finishListening`, i.e. after the *first* recording cycle completes. The very first thing the app ever speaks — the greeting, in `startSession()` — ran before that had ever been called even once, so it played under whatever ambient/default audio session iOS + Expo Go left behind after the mic-permission prompt. Fixed by explicitly calling `exitRecordingMode()` right after permission is granted, before `startSession()` runs (`app/conversation.tsx`'s mount effect).

**This only explains the first line, not "inconsistent throughout."** Every line after the greeting already ran with `exitRecordingMode()` called correctly (it happens at the end of every `finishListening`). If later lines in a real conversation are *also* quiet, the toggle itself isn't achieving the speaker route reliably — a different, unconfirmed problem, most likely iOS's PlayAndRecord→Playback route-change having physical latency beyond when `setAudioModeAsync`'s promise resolves (so speech can start into the stale route before it's finished switching, sounding quiet-then-loud within a single line). Needs one on-device listen to tell apart: is only the very first line quiet (fixed), are ALL lines quiet (toggle isn't working, different fix needed), or does a single line start quiet and jump louder partway through (route-change latency — the fix would be a small settle delay after `exitRecordingMode()`, not yet added since it'd be guessing without that observation).

## Adaptive VAD — fixed-dB thresholds broke silence detection entirely (2026-07-19)

Leander reported the app listening for 20-30 seconds after he stopped talking — completely unacceptable, and he asked for the silence hold to be ~1300ms. The number he saw (25-30s) matches `MAX_UTTERANCE_MS` (the hard cap) almost exactly, which means speech WAS being detected but the silence-ending path never fired at all — so simply changing `SILENCE_HOLD_MS` to 1300ms would not have fixed this on its own; that constant only matters once silence is actually detected, and it wasn't.

Root cause: the VAD used fixed absolute thresholds (`SPEECH_DB = -35`, `SILENCE_DB = -40`) tuned once and assumed to hold everywhere. Real rooms/phones have different noise floors — if ambient noise sits above -40dB (a fan, background TV, a phone's own gain characteristics), the recording never reads as "quiet" and the silence branch is permanently unreachable, so every utterance runs to the 25s hard cap. This could not be confirmed further without on-device metering data, so rather than re-guess a different pair of absolute numbers, the approach changed to be scale-relative instead:

- **Calibration**: the first `CALIBRATION_MS` (300ms) of each recording samples the room's actual noise floor (tracks the minimum metering level seen).
- **Adaptive thresholds**: speech = floor + `SPEECH_MARGIN_DB` (12dB); silence = back down to floor + `SILENCE_MARGIN_DB` (6dB). Both relative to the calibrated floor, not an absolute number, so it self-adjusts to whatever room/device it's running in.
- **`SILENCE_HOLD_MS` set to 1300ms** per Leander's explicit ask — this is the correct knob now that detection can actually fire.
- **Diagnostic logging added** (`[vad] t=...ms level=...dB floor=...dB speech=...`, throttled to ~400ms, plus a `console.warn` if the 25s hard cap is ever hit again) since this class of bug is invisible without seeing the actual metering numbers a real device produces — if the adaptive approach still misbehaves, the Metro terminal will show why instead of another guess-and-ship cycle.
- **Known open risk, explicitly not verified**: `recorderState.metering`'s value range/scale from `expo-audio` was never confirmed against real device output (only inferred from the -35/-40 constants the app shipped with originally). If the scale isn't dBFS-like, the dB margins above may still need adjusting — the log output will make this immediately visible on the next device run.

## Word dictionary, progress hub, spoken meta-commands (2026-07-18)

Leander wanted a profile/hub screen tracking a per-word dictionary, plus the ability to say things like "continue" or "progress" naturally mid-conversation instead of only tapping chips. Scoped to three pieces this round (a fourth — homework grading and a weekly recap quiz — was explicitly parked for later, since "what counts as homework" and "what the weekly quiz actually is" both need their own design pass once there's real usage data to look at).

- **Word-level dictionary (new data model, distinct from phraseStore).** Claude's JSON contract (`claudeJson.ts`) now returns a `words: [{word, meaning}]` breakdown of every `spanish_phrase`, parsed defensively (malformed entries dropped, not fatal — see `parseWords`). New `wordStore.ts` persists these separately from whole learned phrases, deduped by normalized word (accents/case ignored) via the existing `normalize()` from `similarity.ts`. A repeated word increments `timesSeen` but keeps its *original* spelling/meaning rather than being overwritten — Claude's gloss should be stable, and the first version is as good as any later one. Recorded via `recordReplyWords` at both call sites that produce a new target (initial teach + extend), best-effort (a storage failure must never break the conversation).
- **Progress hub (`app/profile.tsx`).** Stats row (phrase count / word count / average score), a "Needs practice" section auto-flagging phrases scored below 80 (no separate homework-assignment step — the threshold itself is the flag, sorted worst-first), and "Mi diccionario" as a tappable word-chip grid (tap to hear, count badge for repeated words). Links out to the existing Mis frases screen for full phrase browsing rather than duplicating it. Home screen's secondary button now points here instead of straight to Mis frases (progress is the more useful hub; phrases stays one tap further in).
- **Spoken meta-commands (`nextCommand.ts`), reusing the existing chip infrastructure rather than building a parallel system.** During the `choosing` phase (after a phrase completes), the mic now also accepts spoken Spanish commands — "continúa"/"sigue" (build on it), "dónde"/"cuándo"/"como me siento" (element-specific extends), "nuevo tema" (new topic), "progreso" (jump to the hub) — matched by *exact* normalized-transcript equality against a phrase list, never substring, so a real sentence containing "más" isn't misheard as a command. Unrecognized speech during `choosing` falls through to `handleEnglishRecording` (treated as a fresh topic in English). The first time `choosing` is reached in a session, the coach speaks a one-time bilingual tip ("Quick tip: from here you can just talk to me. Say 'continúa'... Continúa. Progreso.") — gated by a `hasTaughtNextCommands` flag that persists for the app's lifetime, not per-conversation, so it doesn't repeat every session.
- **Phase-race bugs found by adversarial review, not by tests — and one is a real regression that shipped earlier this session.** `finishListening` and `cancelRecording` both used to read the store's live `phase` to decide "was this a repeat attempt / where do I return after silence" — but by the time either runs, `setRecording()` has already flipped `phase` to `'recording'`, so that read is always stale. The earlier commit this session that added `phase === 'awaiting-repeat' && …` to `wasRepeat` (trying to distinguish choosing-phase speech from a real repeat) made this a live bug: **every repeat attempt since that commit was silently routing to `handleEnglishRecording` instead of being scored** — pronunciation scoring was broken, not just the new choosing flow. Fixed at the root: added `preRecordPhase` to the store, stashed by `setRecording()` before it overwrites `phase`, and both `cancelRecording` and `wasRepeat` now read that instead of guessing. The screen's `choosingActive` (chip visibility) derives from the same field so there's one authority, not two guesses that can drift.
- **Unverified from here, flagged rather than asserted:** the choosing-phase Whisper call uses an `'en'` language hint (to transcribe once instead of twice, for latency) even though the commands themselves are Spanish words ("progreso", "continúa"). Whether Whisper reads short Spanish command words correctly under an English hint can't be confirmed without running on-device — if "progreso" starts a new phrase instead of opening the hub, flip the hint to `'es'` in `app/conversation.tsx`'s `finishListening`.

## Spanish/English voice mismatch (2026-07-18)

Leander heard the Spanish and English voices as two different women, despite both routing through OpenAI `nova`. Two contributing causes, both fixed:

- **Silent fallback wasn't logged.** `speakNova` in `tts.ts` catches any TTS API/network/playback failure and silently falls back to the free robotic device voice — with zero logging, so a failed call for one language would swap in a completely different-sounding voice mid-conversation without any visible sign why. Added a `console.warn` on that path so it shows up in the Metro terminal if it happens again.
- **The per-language `instructions` text described two different characters instead of one.** "A friendly Colombian woman" (es) vs. "an encouraging language buddy" (en) are different enough character descriptions that `gpt-4o-mini-tts` can drift pitch/pacing/energy across them even under the same `voice` param. Rewrote both to share a `VOICE_ANCHOR` sentence ("You are the same warm, upbeat young bilingual woman throughout...") with only the accent instruction varying per language.
- **Cache invalidation for instruction changes.** The on-device MP3 cache is keyed by text+lang+voice, which doesn't know when the `instructions` wording changes — old cached audio (recorded under the old instructions) would keep playing forever otherwise. Added `VOICE_PROFILE_VERSION` (bumped to 2 with this change) into the cache filename, so any future instructions tweak auto-invalidates by orphaning old files rather than needing a manual cache clear. Old v1 files are just harmless dead weight in the cache dir.

## Conversational feel tuning + choosing-phase regression fix (2026-07-18)

Leander wanted the app to feel like a live conversation (listen continuously, respond the instant he stops talking) instead of a record → transcribe → reply cycle. True streaming STT (partial transcripts while speaking, no separate upload step) needs either a native speech module or raw-PCM streaming to a service like Deepgram — both require leaving Expo Go for a development build. He chose to stay in Expo Go for now and tune the existing VAD-based flow instead:

- **`SILENCE_HOLD_MS` 1300ms → 650ms** and **`LISTEN_START_DELAY_MS` 550ms → 250ms** (`app/conversation.tsx`): she decides you're done talking twice as fast, and starts listening again twice as fast after she finishes speaking. `MIN_UTTERANCE_MS` dropped 700→500ms to match. These are the cheap, reversible levers; a real streaming rewrite is still on the table if this isn't enough — surfaced to Leander as a future decision, not decided here.
- **Fixed a regression from the previous session**: the `choosing` phase (chips after completing a phrase) disabled auto-listen, forcing a mandatory tap — the opposite of "responds as soon as I finish talking." Auto-listen and the mic now work during `choosing` too; talking again is a faster alternative to tapping a chip. Talking during `choosing` routes through `handleEnglishRecording` (a fresh topic), not `handleRepeatRecording` — `finishListening`'s `wasRepeat` check is now gated on `phase === 'awaiting-repeat'`, not just `currentTarget !== null` (which was still set to the just-completed phrase during `choosing`). `cancelRecording` (silence recycle) also now returns to `choosing` instead of incorrectly falling back to `awaiting-repeat`.

## Live-testing feedback round (2026-07-18)

Four changes from Leander's first real conversation with the app:

- **She says the phrase before asking you to say it.** Speaking order everywhere is now Spanish phrase → English coach line (was coach line → phrase), and the transcript bubbles match. The system prompt now tells Claude the coach line is spoken *after* the phrase, so it's written as a follow-up invitation ("That means X — your turn!") instead of an introduction.
- **The grade explains itself.** New `diffWords` in `similarity.ts`: LCS alignment of normalized tokens, marking each target word hit/missed while preserving original spelling for display. Any score with a missed word shows the target phrase with misses underlined in red plus "I heard: …" (the Whisper transcript). Word order matters; pure-punctuation tokens can't be "missed".
- **Celebration, not a flat number.** Score rows spring-pop in (bouncier when ≥80); perfect scores get a sunshine-bordered card, 🎉, and one of five rotating celebration lines so "¡Perfecto!" never sounds canned.
- **You choose what's next.** The app no longer auto-extends after a completed phrase. New `choosing` phase with tappable chips: ➕ Build on it, 📍 Add where, 🕐 Add when, 😊 Add a feeling, 🔄 New topic. Extend chips pass the chosen element into Claude's EXTEND instruction; New topic clears the target and asks "¿Qué más?". Auto-listen stays off while choosing (the phase isn't in the auto-listen list), so the mic doesn't grab the silence while you decide.

## Mis frases review screen (2026-07-18)

The MVP saved learned phrases to AsyncStorage but had no way to see them. Now there is: a **"Mis frases"** screen (`app/phrases.tsx`), newest-first, each card showing the Spanish, the English, and the best-score ring in the same color bands as the conversation screen. Tap a card to hear the phrase in the natural nova voice (replays are free — the MP3 is already cached from when it was learned), 🐢 replays it slow (same cached file at 0.6×), ✕ removes it (`removePhrase` added to the store). The Home screen gets a turquoise-outline secondary button with a live count that only appears once at least one phrase exists — a brand-new user still sees exactly one button. Deliberately not built yet: spaced-repetition scheduling or a quiz mode; this is the browse/replay layer that the trip actually needs.

## Testing

- **jest-expo** preset; AsyncStorage uses its official jest mock (wired in `jest.setup.js`).
- Unit tests cover the three brief-mandated areas: the similarity scorer (normalization, Levenshtein, score bands), Claude JSON parsing (fences, prose, malformed input, missing fields), and phrase persistence (round-trip, dedupe, corrupted storage).
- The UI screens and network services are intentionally untested — MVP tests target the pure logic, per the brief.

## Explicitly skipped (per the brief)

Accounts, onboarding beyond the mic ask, gamification, settings, backend/proxy, Android polish, offline dictionary.

## Hear yourself: playing back your own recording (2026-07-20)

Leander asked to be able to hear his own recorded attempt played back, citing a specific example ("I am talking on the phone with my amigo Chris") that was **English input**, not a Spanish repeat attempt.

**The recorder reuses one file path.** `useAudioRecorder` creates a single long-lived `AudioRecorder` instance per screen mount; `expo-audio`'s native side (`AudioUtils.createRecorder`/`createRecordingUrl`) picks the file's UUID-based name once, at construction, not per-recording. So every `recorder.record()` call on the same screen instance writes to the same file, and the next recording overwrites whatever was there. Confirmed by reading `node_modules/expo-audio/ios/AudioUtils.swift` and `AudioModule.swift` directly rather than assuming. This means any "hear it back" feature has to copy the clip out to a stable, uniquely-named file immediately after each recording, or it will be silently clobbered by the very next thing the user says.

**New `src/services/attemptAudio.ts`**: `persistAttemptAudio(sourceUri, entryId)` copies the recorder's current file into `Paths.cache/attempt-audio/<entryId>.m4a` using `expo-file-system`'s synchronous `File.copy()`. Both `handleEnglishRecording` and `handleRepeatRecording` in `useConversation.ts` now call this right after transcription succeeds, and attach the resulting URI to the relevant `TranscriptEntry` as `attemptAudioUri` (on `user-english` entries for English input, and on both `user-attempt` and the following `score` entry for a Spanish repeat, since the score card already surfaces per-word "why" detail in the same place). Playback reuses `tts.ts`'s existing `playFile` helper (now exported) rather than duplicating `createAudioPlayer` boilerplate.

**A "Hear yourself" pill** appears on the `user-english` bubble and on the `score` card in `conversation.tsx`, matching the existing `replayPill`/Ionicons style. `user-attempt` bubbles don't get their own button since the score card right below already offers one for the same clip, avoiding a duplicate control for a single recording.

**Known limitation, told to Leander directly**: this only captures recordings made after this change shipped. The specific "amigo Chris" clip he asked about was already gone (overwritten before this code existed) by the time this was built — nothing retroactively recovers it. He'll need to record a fresh utterance to test the feature.

**Left alone, flagged not fixed**: `attempt-audio/` grows unboundedly on-device (one file per English/Spanish recording, forever). Not cleaned up this round since it wasn't asked for and doesn't block the feature; worth a TTL or a cap tied to transcript/session length if it becomes a real storage concern.

**Failure mode made visible, not silent**: `persistAttemptAudio`'s failure path now `console.warn`s (matching how `tts.ts`'s `speakNova` logs its device-voice fallback) instead of swallowing the error, since a silent catch here would make "the button just doesn't show up" undebuggable during on-device testing.

Separate from, and unrelated to, the UI overhaul v2 work above — a distinct feature request, not a redesign-scope item, and does not change anything documented in that section (its "zero changes to useConversation.ts" claim is accurate as of `c8d4742`; this is a later, distinct edit to that file).

**REVERSED, same day.** After using it, Leander said the feature wasn't actually useful and clarified the real product goal: minimal friction from "phone in pocket" to hearing the Spanish translation spoken back, fast (his example: ordering in Spanish at a Chick-fil-A or barbershop). Playing back the user's own recording adds a step in exactly the moment that's supposed to feel instant, and doesn't add pronunciation-level insight anyway, since scoring is Whisper-transcript similarity, not acoustic analysis of the audio. Fully removed: `src/services/attemptAudio.ts` deleted, `attemptAudioUri` field and both `persistAttemptAudio` call sites removed from `useConversation.ts`, the "Hear yourself" button and its styles removed from `conversation.tsx`. Kept: the Replay/Slow buttons on Spanish cards (those play the TARGET phrase, the thing being taught, not the user's own voice) and their per-card bug fix from earlier today.

## Wordmark, tagline, and remaining brand references (2026-07-20)

**Wordmark**: earlier in the day, Leander rejected a two-tone "pron"/"to" split (matching `mockups-v2.html`) as reading like two separate syllables, so it was flattened to one solid color. Later he asked for the opposite: every letter of "Pronto" colored, cycling through the zócalo's own 3-color order (coral, sunshine, turquoise, per `ZocaloMark.tsx`), repeating after 3 letters, so with 6 letters each color appears exactly twice (P=coral, R=sunshine, O=turquoise, N=coral, T=sunshine, O=turquoise). Not a contradiction: the earlier objection was specifically about a 2-way split reading as two words; a 6-way per-letter cycle is decoration, not a semantic break. Also bumped the Home wordmark font size (20 to 30) per "the name is too small."

**Tagline** changed from "Spanish for right now." to "Speak Spanish Pronto." per direct request.

**Remaining "Qué Onda" references removed**: `app.json`'s two `NSMicrophoneUsageDescription`/`microphonePermission` strings, and the Claude system prompt's `You are "Qué Onda"` identity line, both swapped to Pronto.

**Judgment call, not directly requested**: `GREETING_ES` (`'¿Qué onda?'`, the very first thing the app ever says) was swapped to `'¿Qué más, pues?'`. Leander only asked to remove the app NAME, and "¿Qué onda?" is a legitimate standalone Spanish greeting, not purely branding, but it's also the spoken form of the old name and would be the first line heard in every demo. Flagged to Leander as a judgment call, not silently changed. Pre-existing, not a new issue: the ES/EN greeting pair (`GREETING_EN = 'What are you doing right now?'`) already didn't literally translate `'¿Qué onda?'`, and still doesn't literally translate `'¿Qué más, pues?'` either — optional to tighten, not a regression introduced here.
